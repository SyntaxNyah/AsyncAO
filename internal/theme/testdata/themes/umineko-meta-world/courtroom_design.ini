; The AO2 half of the fixture theme. AsyncAO never writes this file in the
; normal path — every edit the editor makes lands in asyncao_theme.ini beside
; it — so it is here to prove exactly that: it must come back byte-identical
; after a save.
courtroom   = 0, 0, 944, 600
ic_chatlog  = 16, 312, 476, 268
ao2_chatbox = 16, 464, 668, 176
ms_chatlog  = 520, 60, 400, 240

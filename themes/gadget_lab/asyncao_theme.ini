; ===========================================================================
; gadget_lab - a STANDALONE AsyncAO theme (format 1).
;
; Drop this folder into your themes/ folder. It carries no courtroom_design.ini
; of its own: [theme] base = default puts it over AO2's stock design, and every
; rect below is laid on top of that as an [overrides] edit.
;
; ZERO BYTES OF ART. No [media] section, no fonts/ directory, no sounds/
; directory. Every surface is a palette colour, a shape, a gradient, a
; procedural generator tile or plain text. Nothing was copied, embedded, traced
; or sampled from any third-party image: the numbers in the comments are
; MEASUREMENTS (proportions, ratios, colour readings), and a measurement is the
; only thing that crossed over.
;
; THE MOOD. A hand-built lab bench after midnight: warm-black void, a tan
; perfboard deck bolted across the top with a small cold-cathode digit counter
; on it, rust framing on the panels, one amber rule under the dialogue, and a
; polaroid of a lab member pinned to the corner of the stage.
;
; DESIGN CANVAS: 714 x 668. Every number in [overrides] is arithmetic on that
; canvas and rides fitDesignCanvas, so nothing here assumes a window size.
;
; ANCHORING: every [element.*] sets an `anchor`, so re-arranging the layout (or
; applying a layout preset over it) moves the decoration with the widget it
; dresses, by construction. An anchored `rect` is a signed DELTA on all four
; components (RULING R1): X = anchor.X + rect.x, W = anchor.W + rect.w, and
; all-zero means exactly the anchor's rect. Every explicit size below is written
; that way and its resolved pixel size is in the trailing comment.
;
; NO full_canvas KEY. `full_canvas` belongs to [preset] fragments, not to a
; theme. The four atmosphere overlays here are anchored to `courtroom` with an
; all-zero rect instead, which is the theme-side spelling of the same thing and
; needs no allow-list.
;
; Free text is percent-encoded on exactly four characters: % -> %25, ; -> %3B,
; LF -> %0A, CR -> %0D. Nothing else is escaped.
; ===========================================================================

[theme]
format       = 1
revision     = 1
name         = Cold Bench
author       = AsyncAO project
license      = procedural, AGPLv3-compatible
credit       = All original. Zero bytes of third-party art, font or audio ship here%3B every surface is a colour, shape, gradient, generator tile or text. Geometry derived from measurements only. No borrowed letterform, no borrowed mark, no borrowed number.
description  = A hand-built lab bench at 3am%3B perfboard deck, cold-cathode counter, rust framing, one polaroid pinned to the stage.
min_client   = 1.90.0
base         = default
subtheme     =
generated_by = hand-authored

; ===========================================================================
; [overrides] - 60 AO2 slot keys, laid over the stock design.
;
; BAND MAP (714 x 668)
;   y   0.. 42  INSTRUMENT RAIL. Decoration only - no widget lives here, so the
;               deck reads as hardware bolted across the top rather than as a
;               toolbar. The digit counter sits centred on it.
;   y  42..462  STAGE, 560 x 420 at x 117. 560/420 is EXACTLY 4:3 - not a style
;               choice: the viewport blits without preserving aspect, so a
;               widescreen stage would stretch every server's background art.
;   y  42..462  x 0..117: THE RACK. The client buttons and the judge strip,
;               stacked down the left on an exposed board. Below y 338 the rack
;               is bare board and one cable - that is the clutter.
;   y 462..668  THE BENCH, full width, five rows.
;
; Keys that are fixed (courtroom / showname / message / music_name - they ride
; their parent) or inert (player_list, casing, effects_dropdown, sfx_remove,
; mute_button, left/right_evidence_icon, clock_0..4) are absent on purpose: an
; override on those is a ghost box. The asyncao_* opt-in rows are absent too -
; inside the AO2 design canvas this theme stays pure AO2, and its own extras
; live in [chrome] and in elements outside the canvas.
; ===========================================================================
[overrides]

; --- THE STAGE. 4:3, offset right so the rack has a home. The 37 px strip left
; --- over on the right is deliberate: it is the gap a bench always has between
; --- the last board and the edge of the table.
viewport = 117, 42, 560, 420

; --- THE TEXTBOX. Width 0.9 x 560 = 504 (even, so the side gaps are 28/28).
; --- Height 118, which puts the panel at 504 x 118 and leaves the bottom edge
; --- 8 px above the stage floor: 462 - 8 - 118 = 336. AO2's chatbox rect
; --- INCLUDES the showname band, so the nameplate ratio is baked into this one
; --- rect. 504 also clears the widest reference `message` child (484 px),
; --- which does not rescale with an overridden ao2_chatbox.
ao2_chatbox = 145, 336, 504, 118

; --- THE RACK: client buttons, 105 px wide at x 6, on a 26 px pitch.
change_character = 6, 46, 105, 22
reload_theme     = 6, 72, 105, 22
call_mod         = 6, 98, 105, 22
settings         = 6, 124, 105, 22
evidence_button  = 6, 150, 105, 22
pair_button      = 6, 176, 105, 22

; --- ...and the judge strip under them. The bars flank their +/- buttons
; --- instead of sitting under them, which is what fits a 105 px rack.
defense_minus     = 6, 208, 10, 10
defense_bar       = 18, 208, 81, 10
defense_plus      = 101, 208, 10, 10
prosecution_minus = 6, 222, 10, 10
prosecution_bar   = 18, 222, 81, 10
prosecution_plus  = 101, 222, 10, 10
witness_testimony = 6, 238, 105, 22
cross_examination = 6, 264, 105, 22
guilty            = 6, 290, 105, 22
not_guilty        = 6, 316, 105, 22

; --- THE SHOUT ROW, on the stage directly above the textbox and flush-left with
; --- it. It stops at x = 501 so it never runs under the polaroid corner.
hold_it          = 145, 304, 58, 26
objection        = 207, 304, 58, 26
take_that        = 269, 304, 58, 26
custom_objection = 331, 304, 58, 26
realization      = 393, 304, 52, 26
screenshake      = 449, 304, 52, 26

; --- BENCH ROW 1: the IC input line, full width. The widget a layout may never
; --- lose, so it gets the whole first row.
ao2_ic_chat_name    = 6, 466, 120, 24
ao2_ic_chat_message = 130, 466, 578, 24

; --- BENCH ROW 2: IC toggles, then the music rail.
; --- `immediate` and `pre_no_interrupt` are the same control under two AO2
; --- spellings, so they share one rect deliberately.
pre              = 6, 494, 40, 20
immediate        = 50, 494, 78, 20
pre_no_interrupt = 50, 494, 78, 20
flip             = 132, 494, 40, 20
additive         = 176, 494, 62, 20
guard            = 242, 494, 50, 20
showname_enable  = 296, 494, 90, 20
slide_enable     = 390, 494, 52, 20
music_label      = 452, 494, 34, 20
music_slider     = 490, 494, 218, 20

; --- BENCH ROW 3: the IC dropdowns, then the SFX rail.
text_color       = 6, 518, 70, 20
sfx_dropdown     = 80, 518, 84, 20
emote_dropdown   = 168, 518, 94, 20
iniswap_remove   = 266, 518, 20, 20
iniswap_dropdown = 290, 518, 80, 20
pos_remove       = 374, 518, 20, 20
pos_dropdown     = 398, 518, 56, 20
sfx_label        = 458, 518, 28, 20
sfx_slider       = 490, 518, 218, 20

; --- BENCH ROW 4: the OOC line, then the blip rail.
ooc_chat_name    = 6, 542, 96, 20
ooc_toggle       = 106, 542, 54, 20
ooc_chat_message = 164, 542, 290, 20
blip_label       = 458, 542, 28, 20
blip_slider      = 490, 542, 218, 20

; --- BENCH ROW 5: four panels across the bottom, 96 px tall.
; --- server_chatlog and ms_chatlog share one rect because ooc_toggle swaps
; --- between them (courtroom.cpp:1013) - the only deliberate overlap here.
emote_left        = 6, 566, 20, 96
emotes            = 28, 566, 172, 96
emote_right       = 202, 566, 20, 96
ic_chatlog        = 230, 566, 204, 96
server_chatlog    = 442, 566, 110, 96
ms_chatlog        = 442, 566, 110, 96
music_display     = 560, 566, 148, 20
music_search      = 560, 590, 124, 20
switch_area_music = 688, 590, 20, 20
music_list        = 560, 614, 148, 48

; ===========================================================================
; NO [rotations] SECTION, and the reason is worth writing down.
;
; A rotation is a draw-only CopyEx on TEXTURE-BACKED chrome. The two keys this
; theme would actually want askew - the nameplate and the polaroid - are exactly
; the two that cannot take one: `showname` draws through the text path and
; ignores the angle, and it is a fixed key that applyRectOverrides drops anyway%3B
; and the polaroid is four separate elements whose registration would drift the
; moment each one rotated about its own centre. A tilted photo that comes apart
; is worse than a straight one. If you want the tilt, put `rot = 3` (about 4 deg)
; on showcase_card, showcase_well, showcase and showcase_cap TOGETHER and check
; the seams yourself.
; ===========================================================================

; ===========================================================================
; [palette]
;
; Image-graded from the reference set. Tolerance +/-8-14 per channel, which is
; why nothing here depends on an exact value: every one of them is a large flat
; field.
;
; The one correction that shapes the whole theme: THE VOID IS WARM. Every dark
; region in every reference is brown-cast and lifted (chassis front ~#16120F,
; desk shadow ~#0F0C0A, the microwave shelf's darkest ~#151714). A neutral
; near-black reads dead and digital against those.
; ===========================================================================
[palette]
text     = #e5dfd5      ; MEASURED - warm off-white. Dialogue is calm, mixed-case
                        ; and undistorted: the glow belongs to the instruments.
panel    = #0e0b09      ; MEASURED - the warm, lifted void. Never neutral black.
panel_hi = #1a1715      ; MEASURED - instrument chassis, warm-neutral. NOT the
                        ; cool-green #131714%3B that value exists in exactly one
                        ; reference (an ambient shelf) and is used once, below,
                        ; on the two input wells and nowhere else.
accent   = #f07a22      ; MEASURED - cold-cathode discharge. Amber, not red.
danger   = #dc1c22      ; MEASURED - the retro-web numeral red. The ambient
                        ; alert flash uses #8e1a1c, which is deeper.

; ===========================================================================
; [chrome] - AsyncAO surfaces OUTSIDE the AO2 design canvas.
; Instrument-panel geometry: rectilinear, no rounded blobs, no anti-aliased
; radius. MEASURED FINDING, and it is the thing that keeps this out of generic
; cyberpunk: there is no milled bezel and no 45-degree chamfer anywhere in any
; reference. A future gadget is an UNFINISHED PROTOTYPE - a plain dark box with
; one lit top rail and an exposed board. So: sharp, tier 0.
; ===========================================================================
[chrome]
shape      = sharp
shape_tier = 0
tabbar_hex = #1a1715    ; chassis, one step above the void - the same step the
                        ; references show between box front and desk shadow.

; ===========================================================================
; [fonts] - CLASS HINTS, not files. Nothing is bundled, so each value names a
; generic family class the resolver ladder can satisfy on any machine. No
; proprietary face is named, referenced or shipped, and no letterform in this
; theme is drawn by a generator - a digit table inside a generator is a font in
; disguise, which is the same IP line by the back door.
;
; TWO REGISTERS, and the split is a measured finding rather than a taste:
;   TECHNICAL GOTHIC - labels, dialogue and lists. The reference UI register is
;                      a narrow, tall, slightly condensed grotesque, uppercase
;                      with wide tracking for system readouts.
;   MONO             - counters and readouts ONLY. Every counter in this theme
;                      is zero-padded, and zero-padding is only honest in a face
;                      whose digits share one advance.
; ===========================================================================
[fonts]
Lab Gothic = sans-serif
Lab Mono   = monospace

[fontbind]
showname       = Lab Gothic
message        = Lab Gothic
ic_chatlog     = Lab Gothic
server_chatlog = Lab Gothic
music_list     = Lab Gothic
area_list      = Lab Gothic
player_list    = Lab Gothic
friends        = Lab Gothic
notes          = Lab Gothic
music_name     = Lab Mono
debug_log      = Lab Mono

; ===========================================================================
; NO [media] SECTION.
; This theme ships no image, no font file and no audio, so there is nothing to
; declare. If you drop a character animation onto [element.showcase] the editor
; writes the entry for you - it looks like this:
;
;   [media]
;   mychar = asyncao/media/mychar.webp, 1843200, 9f2c1a4b8e0d1177
;
; See CHARACTERS.txt.
; ===========================================================================

; ===========================================================================
; [clock.N] - two authoring groups. Clock 0, the shared anchor, is never
; declared and carries nothing here.
; ===========================================================================
[clock.1]
speed_pct = 100     ; "INSTRUMENT" - everything that glows. One knob for the
                    ; whole register: drop it to 40 for a lazier gas discharge,
                    ; or pause it in the editor to inspect a frame. The per-
                    ; element phase_ms offsets are arithmetic on this clock, so
                    ; they survive the change.

[clock.2]
speed_pct = 45      ; "BENCH" - the slow one. Dust over the bench, and the
                    ; breath behind the polaroid.

; ===========================================================================
; [effect.*] - five bindings, and the ONE deliberate omission is the important
; line here: there is NO effect on `message` and none on `ao2_chatbox`. The
; measurements are explicit that dialogue is warm, mixed-case, calm and
; undistorted, and that the glow belongs to the instruments. Everything else in
; this file glows%3B the dialogue earns its contrast by not doing it.
;
; These five are periodic, so they note the pacing census for the session. Delete
; them and the theme is static except for the counter pulse and the bench dust.
; ===========================================================================

[effect.showname]
; The nameplate is a lit cell of the same instrument, so the name breathes with
; the rest of the register - same clock, so one knob slows both.
effect    = glow
color     = #f07a22
period_ms = 2500
amp_pct   = 30
clock     = 1

[effect.objection]
; The shout trio are the alert lamps: the UI red (#dc1c22), not the deeper
; ambient one (#8e1a1c) that alert_flash uses. Both are measured, and they are
; different readings of different objects.
effect    = glow
color     = #dc1c22
period_ms = 1800
amp_pct   = 40
clock     = 1

[effect.hold_it]
effect    = glow
color     = #dc1c22
period_ms = 1800
amp_pct   = 40
clock     = 1

[effect.take_that]
effect    = glow
color     = #dc1c22
period_ms = 1800
amp_pct   = 40
clock     = 1

[effect.music_display]
; The now-playing plate is the one panel that shows a VALUE, so it gets the
; instrument register instead of the rust one - and a readout breathes.
effect    = breathe
color     = #f07a22
period_ms = 6000
amp_pct   = 18
clock     = 1

; ===========================================================================
; BAND: backdrop - above the courtroom background, below every widget.
;
; PAINT-ORDER CONSTRAINT this whole file is shaped by. The themed pass is:
; backdrop elements -> phaseStage widgets -> MID elements -> phasePanels widgets
; -> overlay elements. ao2_chatbox, showname, message and viewport are
; phaseStage. Therefore an OPAQUE mid element anchored to the chatbox would
; paint ON TOP OF THE DIALOGUE. The two chatbox plates live here in backdrop
; instead, where they show through whenever the AO2 base theme has no chatbox
; skin - which is the stock case, since the default theme ships no chatbox.png.
; Only transparent-centre decoration (a frame, a rule, a keyline) rides mid over
; a stage-phase widget.
; ===========================================================================

[element.void_wash]
; The warm void. One flat blit, visible in every gutter between panels.
kind   = shape
shape  = sharp
band   = backdrop
z      = 0
space  = courtroom
anchor = courtroom
rect   = 0, 0, 0, 0
fill   = #0e0b09

[element.bench_wood]
; MEASURED: the hero reference's whole field is dark warm WOOD GRAIN, not black -
; the instrument sits on a surface, it does not float in a void. Four warm values
; as the woodgrain generator's four colour slots, across the bench band only.
kind       = gen
generator  = woodgrain
gen_params = tint=#1c1712, bg=#0e0b09, accent=#2e2018, shadow=#0a0806, pitch=3, pct=0
band       = backdrop
z          = 6
space      = courtroom
anchor     = courtroom
rect       = 0, 462, 0, -462      ; 714 x 206: the bench under the stage
fit        = tile
fill       = #0e0b09              ; degrade target: the void, never a hole

[element.void_halftone]
; MEASURED, and it replaces the scanlines a first pass would have assumed: the
; period-correct texture for a large flat field here is a HALFTONE WASH. Zero
; scanlines exist in any reference, and the one screen reference is rendered
; clean with a halftoned outer field. Warm #241c12 at 18%.
kind       = gen
generator  = halftone
gen_params = dot=6, angle=0, tint=#241c12, bg=#00000000
band       = backdrop
z          = 8
space      = courtroom
anchor     = courtroom
rect       = 0, 0, 0, 0
fit        = tile
opacity    = 46
fill       = #00000000

[element.rail_box]
; The instrument rail's chassis: a plain dark box. MEASURED chassis value, and
; MEASURED as a plain box - there is no bevel and no chamfer on it anywhere.
kind   = shape
shape  = sharp
band   = backdrop
z      = 10
space  = courtroom
anchor = courtroom
rect   = 0, 0, 0, -626           ; 714 x 42: the rail
fill   = #1a1715

[element.rail_deck]
; THE PERFBOARD, and this is the theme's signature surface. MEASURED as a tan
; substrate #c0a96e with dark holes #5e4e30 - a LIT board the instrument stands
; on, not a dark backdrop screen. So the mapping is holes-as-ink on board-as-
; field, which is the honest way round: tint = hole, bg = board.
;
; PITCH, derived: the reference hole pitch is cell_w / 6.5 = 58 / 6.5 = 8.9 px on
; a 599 px frame = 1.49% of frame width. On this 714 px canvas that is 10.6 ->
; 11. Every perfboard in this file uses pitch 11 so the two param sets are the
; only two tiles the whole theme rasterises for it.
kind       = gen
generator  = grid
gen_params = pitch=11, size=1, dots=1, pct=0, tint=#5e4e30, bg=#c0a96e
band       = backdrop
z          = 12
space      = courtroom
anchor     = courtroom
rect       = 0, 0, 0, -626       ; 714 x 42, exactly over rail_box
fit        = tile
opacity    = 150                 ; the board catching light, not a poster of one
fill       = #c0a96e             ; degrade target: bare board

[element.rail_parts]
; MEASURED: a 6 px row of discrete-component silhouettes along the deck's front
; edge, alternating dark cylinders #141210 and yellow-orange blocks #d8a83c.
; That row is what makes the deck read as HAND-BUILT rather than manufactured,
; and it is the cheapest single element in the file to say so with.
kind       = gen
generator  = stripes
gen_params = pitch=14, size=6, angle=0, bloom=0, tint=#141210, accent=#d8a83c, bg=#00000000
band       = backdrop
z          = 14
space      = courtroom
anchor     = courtroom
rect       = 0, 34, 0, -662      ; 714 x 6, along the rail's front edge
fit        = tile
opacity    = 220
fill       = #00000000

[element.rail_lip]
; MEASURED: one lighter top rail, #4a4a4c, 1 px. The rail is the box's ONLY
; edge treatment - no bevel, no chamfer, no keyline anywhere else on it.
kind   = shape
shape  = sharp
band   = backdrop
z      = 16
space  = courtroom
anchor = courtroom
rect   = 0, 41, 0, -667          ; 714 x 1, the lit lip
fill   = #4a4a4c

[element.rack_board]
; The left rack is the same board, dark this time: MEASURED tan on a warm-black
; ground at low opacity reads as a prototype board seen edge-on in low light.
; Byte-identical params to every other dark board in this file, so they share one
; texture. Below the judge strip (y 338) this is bare board - that is the clutter.
kind       = gen
generator  = grid
gen_params = pitch=11, size=1, dots=1, pct=0, tint=#c0a96e, bg=#141210
band       = backdrop
z          = 18
space      = courtroom
anchor     = courtroom
rect       = 0, 42, -597, -248   ; 117 x 420: the rack, full stage height
fit        = tile
opacity    = 42
fill       = #141210

[element.rack_cable]
; THE BENCH CABLE. MEASURED off the braided-cord macro: band height 130 px on a
; 576 px frame, strand width 26 px = 20% of band height, crossing pitch 150 px =
; 1.15 x band height. Transferred to a 10 px band that is size = 2 and pitch =
; 12. Rust #8a5038 strand with an amber #f07a22 bloom.
;
; WHAT IS LOST, stated plainly: the reference is a THREE-STRAND braid, strands
; converging and diverging, and there is no braid primitive. A bloomed diagonal
; rope at 105 x 10 px reads as a cable, which is what it is here - it is no
; longer a braid, and no parameter of `stripes` will make it one.
kind       = gen
generator  = stripes
gen_params = pitch=12, size=2, angle=16, bloom=3, tint=#8a5038, accent=#f07a22, bg=#00000000
band       = backdrop
z          = 20
space      = courtroom
anchor     = courtroom
rect       = 6, 404, -609, -658  ; 105 x 10, across the bare rack
fit        = tile
opacity    = 210
fill       = #00000000

[element.chat_panel]
; band = backdrop, NOT mid - see the paint-order note at the top of this band.
; MEASURED chatbox ground: warm-black, at 95% so a little stage shows through.
kind   = shape
shape  = sharp
band   = backdrop
z      = 30
space  = chatbox
anchor = ao2_chatbox
rect   = 0, 0, 0, 0
fill   = #0e0b09f2

[element.name_glass]
; MEASURED: the nameplate is a single cell of the same instrument - smoky brown
; glass #2e2018 at 80%. Backdrop for the same reason chat_panel is.
kind   = shape
shape  = sharp
band   = backdrop
z      = 32
space  = chatbox
anchor = showname
rect   = 0, 0, 0, 0
fill   = #2e2018cc

; ===========================================================================
; BAND: mid - over the stage, behind the panels.
;
; Read the paint-order note on the backdrop band before moving anything into
; here. Rule of thumb this file obeys: over a STAGE-phase widget (viewport,
; chatbox, showname) a mid element must have a transparent centre or must stop
; short of the text%3B over a PANEL widget (the logs, the lists, the inputs, the
; emote grid) mid is BEHIND the content and an opaque plate is correct and safe.
; ===========================================================================

[element.stage_vignette]
; MEASURED and unambiguous: radial, centre 0% -> edge 30% black. The reference
; corners fall to near-black. 30% of 255 = 77 = 0x4d.
kind        = gradient
band        = mid
z           = 0
space       = viewport
anchor      = viewport
rect        = 0, 0, 0, 0
grad_radial = yes
fill        = #00000000
fill2       = #0000004d

[element.bench_dust]
; Dust over the bench, on the slow clock. 1 px specks at 3% density in the
; MEASURED board tan, transparent-backed so the drift can never expose a field
; edge. It sits over the bench only, where it is behind every panel.
kind             = gen
generator        = noise
gen_params       = density=3, size=1, seed=1900, tint=#c0a96e, bg=#00000000
band             = mid
z                = 2
space            = courtroom
anchor           = courtroom
rect             = 0, 462, 0, -462      ; 714 x 206
fit              = tile
opacity          = 20
clock            = 2
effect           = drift
effect_period_ms = 26000
effect_amp_pct   = 12
fill             = #00000000

; --- THE COUNTER -------------------------------------------------------------
; Divergence-meter digit styling, and the size is derived rather than chosen.
;
; MEASURED cell geometry: glass aspect 1 : 3.57, cell pitch such that the glass
; is 90% of it (inter-cell gap 6-10% of pitch, no rules between cells, all cells
; equal width). So for 8 cells in a plate of height H: glass_w = H / 3.57,
; pitch = glass_w / 0.9, span = 8 x pitch. At H = 34: glass_w = 9.5, pitch =
; 10.6, span = 85 px. Plate = 85 + 2 x 5 margin = 95 -> 96, centred:
; (714 - 96) / 2 = 309.
;
; The VALUE is 00000714 - the design canvas width, zero-padded to eight cells.
; MEASURED observation says a long zero-padded decimal counter is the period-
; correct readout format, and an arbitrary original number is the only kind this
; theme is allowed to ship: no constant from any work appears anywhere in it.
;
; Three elements, ONE rect. They share it byte for byte so the digits land inside
; their own plate by construction - no alignment number is written down anywhere,
; which is the only way registration can never drift. Change them TOGETHER.

[element.rail_plate]
; The instrument box the digits sit on. count = 0 is the bevel edge, size = 2 its
; extent, pitch = 0 a flat face: a plain dark box with a 2 px raised lip, which
; is the MEASURED hand-built chassis and explicitly NOT a chamfered bezel.
; `shadow` carries the lit #4a4a4c top rail. fit = nine so the box fills its rect
; while the 2 px lip keeps a fixed device size.
kind       = gen
generator  = plate
gen_params = tint=#c0a96e, bg=#1a1715, accent=#5e4e30, shadow=#4a4a4c, pitch=0, size=2, count=0, pct=60
band       = mid
z          = 4
space      = courtroom
anchor     = courtroom
rect       = 309, 4, -618, -634        ; 96 x 34, centred on the rail
fit        = nine
nine_slice = 2
fill       = #1a1715

[element.rail_glow]
; The discharge halo, UNDER the digits (z 5 against 6) so the amber reads as the
; tubes' own light rather than as a wash over them. MEASURED: the falloff is
; DARKER and REDDER than the stroke, and the light value lives in the stroke's
; core - the opposite polarity to a naive halo. Half a cycle behind the digits,
; which is the desynchronised per-cell breathe for the price of one integer.
;
; WHAT IS LOST: the reference shows real per-tube brightness VARIANCE - at least
; one cell fully dark and the lit ones visibly uneven in the same frame. One halo
; has one brightness. Nothing here recovers that.
kind             = gen
generator        = glow
gen_params       = radius=70, soft=80, tint=#c4551a, bg=#00000000
band             = mid
z                = 5
space            = courtroom
anchor           = courtroom
rect             = 309, 4, -618, -634  ; identical to rail_plate, on purpose
fit              = contain
opacity          = 90
clock            = 1
phase_ms         = 1250
effect           = pulse
effect_period_ms = 2500
effect_amp_pct   = 6
fill             = #00000000

[element.rail_digits]
; The digits themselves, as text in the bound mono class - eight cells, one
; advance each, which is the one thing the tube grid did that type can also do.
; MEASURED stroke colour #f07a22%3B the near-white core #ffce9a that every lit
; stroke carries is not reproducible in a single text draw and is not faked here.
kind             = text
band             = mid
z                = 6
space            = courtroom
anchor           = courtroom
rect             = 309, 4, -618, -634  ; identical to rail_plate, on purpose
text             = 00000714
font             = music_name
size             = 16
color            = #f07a22
align            = center
clock            = 1
phase_ms         = 0
effect           = pulse
effect_period_ms = 2500
effect_amp_pct   = 6

; --- THE CHATBOX SKIN, as four procedural elements ---------------------------
; MEASURED recipe: warm-black panel, a 1 px amber top border with a ~35% glow
; bleeding ~6 px downward, and a small tag cell in the bottom-left corner. The
; panel is chat_panel up in backdrop%3B the three below are the edge treatment,
; and every one of them stops short of AO2's `message` child, whose stock origin
; inside the chatbox is 10, 13. The rule is 1 px tall and the bleed ends at y 8,
; so neither can ever touch a glyph.

[element.chat_frame]
; The deck framing, mode 2 (rail): a hollow 9-slice with a TRANSPARENT CENTRE,
; which is what makes it safe in mid over a stage-phase widget. The 8 px band
; clears the message origin at every canvas this theme resolves to.
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#f07a22, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 10
space      = chatbox
anchor     = ao2_chatbox
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000     ; degrade target: nothing, never a plate over the text

[element.chat_rule]
; MEASURED: 1 px amber top border. This is the theme's single brightest line and
; the whole reason the dialogue needs no other treatment.
kind   = shape
shape  = sharp
band   = mid
z      = 11
space  = chatbox
anchor = ao2_chatbox
rect   = 0, 0, 0, -117     ; 504 x 1, the top rule
fill   = #f07a22

[element.chat_bleed]
; MEASURED: the rule's glow bleeds ~6 px DOWNWARD at ~35%. 35% of 255 = 89 =
; 0x59. A top-to-bottom gradient from that to transparent is the honest single-
; draw form of it. 7 px tall, so it ends 5 px above the message origin.
kind       = gradient
band       = mid
z          = 12
space      = chatbox
anchor     = ao2_chatbox
rect       = 0, 1, 0, -111 ; 504 x 7, under the rule
grad_angle = 64            ; top -> bottom
fill       = #f07a2259
fill2      = #f07a2200

[element.chat_tag]
; MEASURED: a tiny tag cell in the bottom-left corner. Rust #8a5038 - the
; measured "inactive instrument" value, so an inert marker stays in-world
; instead of going grey. 40 x 6 at the corner, well below the message block.
kind    = shape
shape   = sharp
band    = mid
z       = 13
space   = chatbox
anchor  = ao2_chatbox
rect    = 6, 106, -464, -112   ; 40 x 6
fill    = #8a5038
opacity = 220

[element.name_cell]
; ...and the nameplate's amber keyline. A stroke with a transparent fill is the
; smallest possible "cell", and unlike a 9-slice it degrades to nothing at
; showname's stock 46 x 15 instead of swallowing the widget in its own insets.
kind      = shape
shape     = sharp
band      = mid
z         = 14
space     = chatbox
anchor    = showname
rect      = 0, 0, 0, 0
fill      = #00000000
stroke    = #f07a22
stroke_px = 1

; --- THE PANELS --------------------------------------------------------------
; All phasePanels, so mid is BEHIND their content and an opaque plate is correct.
; Every one of them is mounted on the dark board: MEASURED, the shape grammar is
; an exposed hand-built deck, and the cheapest way to say that across a whole
; panel is to tile the board itself under the controls at low opacity, so the
; widgets read as parts soldered to a prototype rather than as chrome on a plate.
;
; RUST FRAMING, not amber. MEASURED #8a5038 is the in-world "inactive
; instrument" value, so the secondary panels stay in-world instead of going grey
; - and it keeps amber scarce enough to still mean something.

[element.log_page]
kind   = shape
shape  = sharp
band   = mid
z      = 20
space  = courtroom
anchor = ic_chatlog
rect   = 0, 0, 0, 0
fill   = #0e0b09f2

[element.log_board]
kind       = gen
generator  = grid
gen_params = pitch=11, size=1, dots=1, pct=0, tint=#c0a96e, bg=#141210
band       = mid
z          = 21
space      = courtroom
anchor     = ic_chatlog
rect       = 0, 0, 0, 0
fit        = tile
opacity    = 36
fill       = #141210

[element.log_counter]
; A page watermark, behind the log's own text, in the other register's red at 15%
; so it can never fight a line - it fills the empty part of the panel the way a
; 1999 hit counter did. Ten cells this time, and again an original number: 668 is
; this theme's canvas height. A LIVE value would need a text-source key the
; format does not have and should not grow.
kind   = text
band   = mid
z      = 22
space  = courtroom
anchor = ic_chatlog
rect   = 0, 0, 0, 0
text   = 0000000668
font   = music_name
size   = 22
color  = #dc1c2226
align  = center

[element.log_frame]
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#8a5038, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 23
space      = courtroom
anchor     = ic_chatlog
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000

[element.ooc_page]
kind   = shape
shape  = sharp
band   = mid
z      = 24
space  = courtroom
anchor = server_chatlog
rect   = 0, 0, 0, 0
fill   = #0e0b09f2

[element.ooc_frame]
; Byte-identical gen_params to log_frame ON PURPOSE, so the content-addressed key
; hashes once and every rust-framed panel in this theme shares one texture.
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#8a5038, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 25
space      = courtroom
anchor     = server_chatlog
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000

[element.ooc_input]
; The two input wells are the only place the MEASURED ambient cool-green #131714
; is used - it exists in exactly one reference (a shelf under a monitor) and
; using it twice, here, keeps it a reading rather than a tint.
;
; The keyline is the retro-web link blue: MEASURED, and it is the one cue that
; tells you which register you are typing into. Out of character is out of world.
kind      = shape
shape     = sharp
band      = mid
z         = 26
space     = courtroom
anchor    = ooc_chat_message
rect      = 0, 0, 0, 0
fill      = #131714cc
stroke    = #5c5cd2
stroke_px = 1

[element.ic_input]
; ...and in character is in world, so this one is amber. Same shape, same weight,
; one colour apart.
kind      = shape
shape     = sharp
band      = mid
z         = 27
space     = courtroom
anchor    = ao2_ic_chat_message
rect      = 0, 0, 0, 0
fill      = #131714cc
stroke    = #f07a22
stroke_px = 1

[element.music_panel]
kind   = shape
shape  = sharp
band   = mid
z      = 30
space  = courtroom
anchor = music_list
rect   = 0, 0, 0, 0
fill   = #0e0b09f2

[element.music_board]
kind       = gen
generator  = grid
gen_params = pitch=11, size=1, dots=1, pct=0, tint=#c0a96e, bg=#141210
band       = mid
z          = 31
space      = courtroom
anchor     = music_list
rect       = 0, 0, 0, 0
fit        = tile
opacity    = 36
fill       = #141210

[element.music_frame]
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#8a5038, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 32
space      = courtroom
anchor     = music_list
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000

[element.display_glass]
; The now-playing plate is a readout, so it gets the instrument register: smoky
; MEASURED glass, amber framing, and the breathe bound in [effect.music_display].
; It is the one panel in the theme that shows a value, and the only one that is
; not framed in rust.
kind   = shape
shape  = sharp
band   = mid
z      = 33
space  = courtroom
anchor = music_display
rect   = 0, 0, 0, 0
fill   = #2e2018e6

[element.display_frame]
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#f07a22, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 34
space      = courtroom
anchor     = music_display
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000

[element.emote_board]
; Same tile as the other boards, one opacity apart. The emote grid is the most
; button-dense surface in the client, which makes it the most convincing place in
; the whole theme to be a circuit board.
kind       = gen
generator  = grid
gen_params = pitch=11, size=1, dots=1, pct=0, tint=#c0a96e, bg=#141210
band       = mid
z          = 35
space      = courtroom
anchor     = emotes
rect       = 0, 0, 0, 0
fit        = tile
opacity    = 46
fill       = #141210

[element.emote_frame]
kind       = gen
generator  = frame
gen_params = pitch=1, size=8, count=2, gap=0, tint=#8a5038, bg=#1a1715, accent=#c0a96e, shadow=#5e4e30
band       = mid
z          = 36
space      = courtroom
anchor     = emotes
rect       = 0, 0, 0, 0
fit        = nine
nine_slice = 8
fill       = #00000000

; ===========================================================================
; THE LAB-MEMBER POLAROID - the character showcase corner.
;
; Six elements, all anchored to the VIEWPORT, pinned to its top-right corner
; with a 14 px inset. Move the stage and the photo goes with it.
;
; GEOMETRY. Card 150 x 176, photo well 130 x 130 square at a 10 px margin, chin
; 36 px. The card proportion is CHOSEN, not measured - there is no polaroid in
; any reference and this file will not pretend otherwise. What IS measured is the
; colour: when a surface in this world is not an instrument, it is warm paper
; #e6e0d8 with #d8cec0 blotches, not more black. That contrast - one pale paper
; rectangle against a warm-black bench - is the whole idea of the corner.
;
; Card absolute rect on the stock stage: 513, 56, 150, 176.
; Well:                                  523, 66, 130, 130.
; Silhouette:                            557, 92,  62, 104.
; Caption:                               523, 202, 130, 22.
; Each is written below as the R1 delta against viewport = 117, 42, 560, 420.
; ===========================================================================

[element.showcase_glow]
; The soft warm halo behind the card, breathing on the slow bench clock. It is
; what stops the pale rectangle reading as a hole punched in the stage.
kind             = gen
generator        = glow
gen_params       = radius=100, soft=90, tint=#f07a22, bg=#00000000
band             = mid
z                = 50
space            = viewport
anchor           = viewport
rect             = 376, 0, -370, -216    ; 190 x 204, centred on the card
fit              = stretch
opacity          = 96
clock            = 2
effect           = breathe
effect_period_ms = 7000
effect_amp_pct   = 18
fill             = #00000000

[element.showcase_card]
; The paper. MEASURED warm-white #e6e0d8 falling to the MEASURED blotch value
; #d8cec0 - a two-stop gradient is the cheapest honest form of "photo-derived,
; soft-focus warm paper".
kind       = gradient
band       = mid
z          = 51
space      = viewport
anchor     = viewport
rect       = 396, 14, -410, -244         ; 150 x 176, the card
grad_angle = 64
fill       = #e6e0d8
fill2      = #d8cec0

[element.showcase_well]
; The photo window: smoky MEASURED glass, so the picture area reads as something
; developed rather than as a hole in the card.
kind      = shape
shape     = sharp
band      = mid
z         = 52
space     = viewport
anchor    = viewport
rect      = 406, 24, -430, -290          ; 130 x 130, square
fill      = #2e2018
stroke    = #5e4e30
stroke_px = 1

[element.showcase]
; ===== THE CHARACTER SHOWCASE =====
; drop your own character gif here
;
; A placeholder silhouette: a standing figure-shaped plate in the MEASURED glass
; brown with a thin amber edge, seated on the bottom of the photo well the way a
; portrait sits in a frame. It is a shape - arithmetic, nothing more.
;
; We ship NO character art. To put your favourite character here, drag any GIF or
; animated WebP onto this element in the theme editor, or set `media` to your own
; file by hand and change `kind` to `image`. The element is deliberately NOT
; locked, so you can also drag its corners or move it somewhere else entirely.
; See CHARACTERS.txt.
kind      = shape
shape     = pill
band      = mid
z         = 53
space     = viewport
anchor    = viewport
rect      = 440, 50, -498, -316          ; 62 x 104, seated in the well
fill      = #1a1715
stroke    = #f07a22
stroke_px = 1
opacity   = 205
fit       = contain                      ; honoured once `media` is set
locked    = no
hidden    = no

[element.showcase_cap]
; The chin. A polaroid's caption is handwritten%3B ours is a label, because the
; only honest thing to write there is what the slot is for. Original phrasing -
; nothing quoted from anywhere, and no lab-member naming scheme from any work.
kind    = text
band    = mid
z       = 54
space   = viewport
anchor  = viewport
rect    = 406, 160, -430, -398           ; 130 x 22, in the chin
text    = DROP A CHARACTER HERE
font    = Lab Mono
size    = 11
color   = #2e2018
align   = center

[element.showcase_frame]
; One rust keyline round the whole card, so the paper has an edge against the
; bright stage art a server can put behind it.
kind      = shape
shape     = sharp
band      = mid
z         = 55
space     = viewport
anchor    = viewport
rect      = 396, 14, -410, -244          ; identical to showcase_card
fill      = #00000000
stroke    = #8a5038
stroke_px = 1

; ===========================================================================
; BAND: overlay - above everything, below editor chrome.
; ===========================================================================

[element.alert_flash]
; The world-line-shift wash, as far as a format with no event axis can carry it:
; a MEASURED deep #8e1a1c at 20%, gated to a shout so it costs NOTHING and notes
; no animation the rest of the time - a failed visible_when returns before the
; page is touched.
;
; What it is not: a 90 ms flash followed by a horizontal row smear. There is no
; event trigger and no per-row displacement of live content in this format.
kind             = shape
shape            = sharp
band             = overlay
z                = 90
space            = courtroom
anchor           = courtroom
rect             = 0, 0, 0, 0
fill             = #8e1a1c
opacity          = 51
effect           = pulse
effect_period_ms = 700
effect_amp_pct   = 100
clock            = 1
visible_when     = shout:objection

[element.room_vignette]
; The bench closing in at the corners, over the whole client this time rather
; than just the stage. Same MEASURED 30% edge, at a strength that darkens a
; corner without ever touching a word of text.
kind        = gradient
band        = overlay
z           = 92
space       = courtroom
anchor      = courtroom
rect        = 0, 0, 0, 0
grad_radial = yes
fill        = #00000000
fill2       = #0000004d

[element.lab_mark]
; One line of uppercase gothic on the rail's left end, wide-tracked the way a
; MEASURED system readout is. Letterspacing is faked with literal spaces - the
; text raster has no per-glyph tracking, and one masthead does not justify one.
;
; ORIGINAL PHRASING, deliberately. No proper noun from any work appears in this
; theme: not in the display name, not on the rail, not on the polaroid caption,
; and no world-line constant in either counter. The folder is named for the mood,
; and the mood is the only thing that crossed over.
kind    = text
band    = overlay
z       = 94
space   = courtroom
anchor  = courtroom
rect    = 10, 12, -514, -650             ; 200 x 18, on the rail
text    = P R O T O T Y P E   B E N C H   0 3
font    = Lab Gothic
size    = 11
color   = #8a5038
align   = left
opacity = 210

; ===========================================================================
; [x.gadgetlab.*] - the reserved vendor namespace. AsyncAO never assigns these a
; meaning and preserves them verbatim on every round trip, which makes it the
; right place for facts the five palette roles cannot hold.
;
; The RETRO-WEB register: the measured second register of this world is not a
; green terminal - zero green pixels exist in any reference - it is a 1999 web
; page. Only its link blue is spent in this theme (on [element.ooc_input])%3B the
; other three are recorded here for anyone re-styling the log panels.
; ===========================================================================
[x.gadgetlab.retroweb]
x_yellow    = #f5e12e        ; MEASURED - page heading
x_violet    = #7e6dae        ; MEASURED - bevelled display type
x_violet_hi = #c8bfe0        ; MEASURED - its lavender bevel highlight
x_link      = #5c5cd2        ; MEASURED - underlined link. SPENT on ooc_input.
x_oschrome  = #d6d3ce        ; MEASURED - the OS window chrome around the page

[x.gadgetlab.instrument]
; Values the theme measured and could not spend: the near-white core down every
; lit stroke, the cool specular on the glass domes, and the unlit cathode wire.
; They are all sub-primitive - a core needs a second pass down a glyph's centre,
; a specular needs an arc, and cathode ghosts need open wire outlines. Recorded
; rather than faked.
x_core      = #ffce9a        ; MEASURED - hot core, 60% down the stroke centre
x_specular  = #c6cbd0        ; MEASURED - the ONLY cool value in the whole set
x_cathode   = #6a5c4e        ; MEASURED - unlit wire, 12%, open outlines
x_mesh_pct  = 9              ; MEASURED - anode mesh over the lower half of a cell
